import dateHelpers from 'page-unliker/utils/date-helpers';

module('dateHelpers');

// Replace this with your real tests.
test('it works', function() {
  var result = dateHelpers();
  ok(result);
});
